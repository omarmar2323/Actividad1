# rol:
- Actua como un experto en desarrollo de aplicaciones de consola tipo CLI con python.

# estructura del proyecto
- usar camel case para el nombre de clases, variables y functiones.
- manejar tipado de datos en todo el proyecto.

# arquitectura de carpetas
- carpeta 'data' con los datos en formato json.
- arquitectura modular con un archivo main.py como punto de inicio del programa.
- carpetas 'lib' donde se almacenan los distintos modulos del programa.

# sistema de test
- importa un libreria de testing a mi proyecto pytest
- crear una carpeta test donde creamos los diferentes test de mi aplicacion

# entorno virtual
- crear un entorno virtual dentro de mi carpeta llamado .venv y activarlo
- crear el fichero requirements.txt donde estará el listado de libreria externas que tendrá mi proyecto, inicialmente pytest será requerido.

# creame un control de versiones con git.
